version: '2'

services:  
     
  backend:
    build: backend/
    ports:
      - "9000:9000"
    environment:
      - APPID=88510c0ca4c7d0e4f0ade76c22e6c641
      - TARGET_CITY=Tampere,fi

  frontend:
    build: frontend/
    ports:
      - "8000:8000"
    environment:
      - ENDPOINT=http://localhost:9000
    links:
      - backend
